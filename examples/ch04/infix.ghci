:load Plus

--# plus
1 `plus` 2
plus 1 2

True `Pair` "something"
Pair True "something"
--# type
:type elem

--# prefix
elem 'a' "camogie"

--# infix
3 `elem` [1,2,4,8]

--# isPrefixOf
:module +Data.List
"foo" `isPrefixOf` "foobar"

--# isInfixOf
"needle" `isInfixOf` "haystack full of needle thingies"
"end" `isSuffixOf` "the end"
